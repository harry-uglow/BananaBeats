#include "symbolTable.c"

int main() {
    int x = 26;
    int *pX = &x;

    symbolTable_t *st = SymbolTable_new();
    SymbolTable_put("twentysix", pX, st);

    int shouldBeX = *((int*)SymbolTable_get("twentysix", st));

    printf("%d\n",shouldBeX);

    int8_t five = 5; // 101
    int32_t fiveHundred = 500; // 111110100
    fiveHundred |= five;

    printf("%d\n", fiveHundred);


    return 0;
}

